# Todo List for (and by) Raiz Dev
- [ ] Add functions for error and warning prints with a standardized message 
format
- [ ] Fix broken tokenizer (and organize the token kind and value handlers)!
- [ ] Add code for debug messages (either by macros or real functions)
- [ ] Implement a parser for the language (you can start simple)!
- [ ] Implement the interpreter (and yeah, start simple)!
> Note: it is better to try implementing a bytecode "compiler" and then, the 
interpreter
